# Deployment Flags - Control what features are visible to clients
# Blog is ready and visible to clients
BLOG_DEPLOYMENT_COMPLETE=true
BLOG_CONTENT_ENABLED=true

# Launch features hidden while you work on them
APP_LAUNCH_ENABLED=false
VOTING_SYSTEM_ENABLED=false
LAUNCH_PAGE_ENABLED=false

# Admin features (keep enabled for your work)
PREMIUM_FEATURES_ENABLED=true
ADMIN_PANEL_ENABLED=true

# Maintenance mode
MAINTENANCE_MODE=false
MAINTENANCE_BANNER=

# Current mode: Blog-only deployment
# Clients see: Blog content, navigation, but no launch features
# When ready: Set APP_LAUNCH_ENABLED=true, VOTING_SYSTEM_ENABLED=true, LAUNCH_PAGE_ENABLED=true
