// app/components/admin/BlogList.tsx
import { Typography } from '@mui/material';

export default function BlogList() {
  return (
    <div>
      <Typography variant="h4">Blog Posts</Typography>
      {/* Blog post list table or cards go here */}
    </div>
  );
}
